[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
prm-spine-exporter = {editable = true, path = "."}

[dev-packages]
black = "==21.9b0"

[requires]
python_version = "3.9"

[scripts]
format = "black -t py39 -l100 src/ setup.py"
check-format = "black --check -t py39 -l100 src/ setup.py"
